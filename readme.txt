This folder is created by Nurul Fatin Syuhada, which contain 2 type of assignments(ARCard & 3D Model)

1)ARCard [Created in Unity with Vuforia]
- apk Folder
contain the build application and can be run through ANDROID only.

-Marker Folder
contain the picture which used as marker to ARCard apps.

- ARCard File Project
contain the file projects of ARCard

2)3D Model [Created in Blender]
divided into separate folder for each 3d models which are Radio and House